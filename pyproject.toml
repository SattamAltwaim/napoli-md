[project]
name = "napoli-md"
version = "0.1.0"
description = "Flask backend and Vue frontend for NAPOLI trajectory analysis."
requires-python = ">=3.10"
dependencies = [
    "Flask>=3.0,<4",
    "flask-cors>=4.0,<5",
]

[tool.uv]
package = false
